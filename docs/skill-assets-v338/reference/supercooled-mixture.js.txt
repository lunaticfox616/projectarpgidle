/* Gem 56 / effect 48. Three annular hits, one outward-moving visual front.
 * The host owns damage, authoritative enemy snapshots, and MZ database mapping. */
(function(G){
 'use strict';
 const spec={slug:'supercooled-mixture',name:'과냉각 혼합물',gemNumber:56,effectNumber:48,category:'attack',element:'cold',range:4,distanceMetric:'euclidean',radii:[1,2,3],ringHalfWidth:.5,impactCount:3,centerHit:false,homing:false,balance:{damage:null,cooldown:null,cost:null},visualProposal:{minimumFlightMs:420,flightMsPerCell:50,ringIntervalMs:260,tailMs:340},catchUpPolicy:'At most three scheduled deadlines; getEnemies(at) supplies each snapshot. No historical positions are invented.'};
 const validCell=c=>!!(c&&Number.isInteger(c.gx)&&Number.isInteger(c.gy)&&c.gx>=0&&c.gx<9&&c.gy>=0&&c.gy<8),cell=c=>({gx:c.gx,gy:c.gy}),validId=id=>typeof id==='string'?id.length>0:typeof id==='number'&&Number.isFinite(id);
 const inCastRange=(a,b)=>validCell(a)&&validCell(b)&&(a.gx-b.gx)**2+(a.gy-b.gy)**2<=spec.range**2;
 function castCells(source){const out=[];if(validCell(source))for(let gy=0;gy<8;gy++)for(let gx=0;gx<9;gx++)if(inCastRange(source,{gx,gy}))out.push({gx,gy});return out;}
 function clampAim(source,aim){if(!validCell(source)||!aim||!Number.isFinite(aim.gx)||!Number.isFinite(aim.gy))return null;if(inCastRange(source,aim))return cell(aim);const ax=Math.max(-1e6,Math.min(1e6,aim.gx)),ay=Math.max(-1e6,Math.min(1e6,aim.gy));let best=null,d=Infinity,sd=Infinity;for(const c of castCells(source)){const cd=(c.gx-ax)**2+(c.gy-ay)**2,cs=(c.gx-source.gx)**2+(c.gy-source.gy)**2;if(cd<d||cd===d&&cs<sd){best=c;d=cd;sd=cs;}}return best;}
 function inRing(center,target,index){if(!validCell(center)||!validCell(target)||!Number.isInteger(index)||index<0||index>2)return false;const r=spec.radii[index],d=(target.gx-center.gx)**2+(target.gy-center.gy)**2;return d>(r-.5)**2&&d<=(r+.5)**2;}
 function ringCells(center,index){const out=[];if(validCell(center))for(let gy=0;gy<8;gy++)for(let gx=0;gx<9;gx++)if(inRing(center,{gx,gy},index))out.push({gx,gy});return out;}
 function living(enemies){if(!Array.isArray(enemies))return [];const ids=new Set();return enemies.filter(e=>{if(!validCell(e)||!validId(e.id)||ids.has(e.id)||e.alive===false||e.dead===true||e.isDead===true||e.hp!=null&&(!Number.isFinite(e.hp)||e.hp<=0))return false;ids.add(e.id);return true;});}
 const targetsInRing=(center,enemies,index)=>living(enemies).filter(e=>inRing(center,e,index));
 const visualSpeed=n=>Number.isFinite(n)?Math.max(.5,Math.min(2.5,n)):1,flightMs=(a,b,speed=1)=>Math.round((420+Math.hypot(b.gx-a.gx,b.gy-a.gy)*50)/visualSpeed(speed));
 const rejected=reason=>({ok:false,reason,done:true,impacts:0,update(){return 0;},cancel(){return false;}});
 function createCast({source,aim,getEnemies,emit=()=>null,onRing=()=>{},onCancel=()=>{},onVisualError=()=>{},startAt=0,speed=1,channelId='supercooled-mixture'}={}){
  if(!validCell(source))return rejected('invalid-source');const landing=clampAim(source,aim);if(!landing)return rejected('invalid-aim');
  if([getEnemies,emit,onRing,onCancel,onVisualError].some(f=>typeof f!=='function')||!Number.isFinite(startAt)||!validId(channelId))throw Error('Invalid supercooled mixture cast arguments');
  speed=visualSpeed(speed);const from=cell(source),flightDuration=flightMs(from,landing,speed),landingAt=startAt+flightDuration,interval=Math.round(260/speed),ringTimes=[0,1,2].map(i=>landingAt+i*interval),waveDuration=2*interval+Math.round(340/speed),endAt=landingAt+waveDuration;
  if(!Number.isFinite(endAt)||landingAt<=startAt||ringTimes.some((at,i)=>i&&at<=ringTimes[i-1]))throw Error('Cast deadlines exceed finite clock precision');
  let index=0,started=false,waveStarted=false,updating=false,cancelled=false,cancelledAt=null,lastNow=-Infinity,currentAt=null,lastVisualError=null;const handles=[];
  function visualCall(fn){try{return fn();}catch(error){lastVisualError=error;try{onVisualError(error);}catch(_){}return null;}}
  function send(phase,at,duration){const event={skillName:spec.name,kind:phase==='flight'?'travel':'stage',supercooledPhase:phase,sourceCell:cell(from),targetCells:[cell(landing)],landingCell:cell(landing),at,duration,ringInterval:interval,stageIndex:0,element:spec.element,damageCategory:spec.category,channelId,visualOnly:true,renderLayer:phase==='wave'?'ground':'foreground',footprint:{center:cell(landing),shape:'annulus',radius:3,cells:[0,1,2].flatMap(i=>ringCells(landing,i))}};handles.push(event);const h=visualCall(()=>emit(event));if(h&&typeof h==='object'&&h!==event)handles.push(h);if(cancelled)for(const e of handles)e.supercooledCancelledAt=cancelledAt;}
  const cast={ok:true,source:cell(from),landingCell:cell(landing),startAt,landingAt,impactAt:landingAt,ringTimes:[...ringTimes],flightDuration,ringInterval:interval,waveDuration,endAt,get done(){return cancelled||index===3;},get impacts(){return index;},get cancelled(){return cancelled;},get lastVisualError(){return lastVisualError;},
   cancel(at=currentAt??(Number.isFinite(lastNow)?lastNow:startAt)){if(cancelled||index===3&&lastNow>=endAt)return false;if(!Number.isFinite(at))throw Error('Invalid cancellation timestamp');cancelled=true;cancelledAt=Math.max(startAt,currentAt??(Number.isFinite(lastNow)?lastNow:startAt),at);for(const e of handles)e.supercooledCancelledAt=cancelledAt;visualCall(()=>onCancel(channelId,cancelledAt));return true;},
   update(now){if(cancelled||updating||!Number.isFinite(now)||now<startAt||now<lastNow)return 0;lastNow=now;updating=true;let processed=0;try{
    if(!started){started=true;send('flight',startAt,flightDuration);}if(cancelled||now<landingAt)return 0;
    if(!waveStarted){waveStarted=true;send('wave',landingAt,waveDuration);}if(cancelled)return 0;
    while(index<3&&ringTimes[index]<=now&&!cancelled){const i=index,at=ringTimes[i];currentAt=at;const targets=targetsInRing(landing,getEnemies(at),i);if(cancelled)break;index++;processed++;const cells=[...new Map(targets.map(e=>[e.gx+','+e.gy,cell(e)])).values()];
     onRing({at,index:i,ringNumber:i+1,totalImpacts:3,radius:spec.radii[i],innerRadius:spec.radii[i]-.5,outerRadius:spec.radii[i]+.5,shape:'annulus',category:spec.category,element:spec.element,damageType:'hit',isHit:true,damage:null,sourceCell:cell(from),landingCell:cell(landing),targets,targetIds:targets.map(e=>e.id),cells,channelId});currentAt=null;
    }return processed;
   }finally{currentAt=null;updating=false;}}
  };return cast;
 }
 if(!Array.isArray(G.WT_CATALOG))throw Error('Load catalog.js before supercooled-mixture.js');
 if(!G.WT_CATALOG.some(s=>s.id===48))G.WT_CATALOG.push({id:48,name:spec.name,slug:spec.slug,skill:{isGem:true,category:spec.category,ele:spec.element,baseSpd:null,damage:null,desc:'과냉각 포션 한 병을 최대 4칸 거리로 던집니다. 착지점에서 반경 1→2→3칸의 도넛형 서리 고리가 차례로 넓어지며, 각 새 고리 안의 적에게 냉기 피해를 한 번씩 주는 공격 스킬입니다. 가운데와 이미 지나간 안쪽 영역은 타격하지 않습니다.',tags:['attack','projectile','cold','aoe'],targetMode:'aimed-annuli',combatPattern:{kind:'supercooledMixture',impacts:3,radii:[1,2,3],centerHit:false}},grid:{kind:'blast',range:4,radius:3,shape:'annulus',distanceMetric:'euclidean'},previewDefaults:{speed:1},profile:{family:'supercooledMixture',scale:1}});
 function previewEnemies(center,source){const out=[];for(const [dx,dy] of [[0,0],[1,0],[-1,1],[2,0],[0,-2],[-3,0],[0,3],[3,-1],[3,3]]){const c={gx:center.gx+dx,gy:center.gy+dy};if(validCell(c)&&(c.gx!==source.gx||c.gy!==source.gy))out.push({id:'C'+out.length,...c,hp:100});}return out;}
 const previous=G.buildWTPreview;G.buildWTPreview=function(item,options={}){if(item.id!==48)return previous(item,options);const source=options.source||{gx:1,gy:4},aim=options.aim||{gx:4,gy:4},landing=clampAim(source,aim);if(!landing)throw Error('Invalid supercooled preview');const enemies=options.enemies||previewEnemies(landing,source),events=[],rows=[],timeline=[],speed=visualSpeed(options.attackSpeed);
  const cast=createCast({source,aim,speed,getEnemies:()=>enemies,emit:e=>{events.push(e);return e;},onRing:hit=>{rows.push({index:hit.index,at:hit.at,from:cell(source),impact:hit.cells,footprint:{center:cell(landing),shape:'annulus',radius:hit.radius,innerRadius:hit.innerRadius,outerRadius:hit.outerRadius,cells:ringCells(landing,hit.index)},element:'cold',stage:{kind:'supercooledRing',label:'서리 고리 '+hit.ringNumber+' · 반경 '+hit.radius+'칸',damageMultiplier:null,targets:hit.targets.map(enemy=>({enemy}))}});timeline.push({at:hit.at,stage:hit.index,label:'서리 고리 '+hit.ringNumber+' · '+hit.targets.length+'명 타격',element:'cold'});}});
  timeline.push({at:0,stage:0,label:'과냉각 포션 투척',element:'cold'});cast.update(0);for(const at of cast.ringTimes)cast.update(at);
  return {events,rows,timeline,duration:cast.endAt+320,source:cell(source),target:cell(landing),landingCell:cell(landing),impactAt:cast.landingAt,flightDuration:cast.flightDuration,ringTimes:cast.ringTimes,ringInterval:cast.ringInterval,waveDuration:cast.waveDuration,targets:living(enemies).map(enemy=>({enemy})),profile:{kind:'blast',range:4,radius:3,shape:'annulus'},attackSpeed:speed,channelInterval:null,motion:0,supercooledMixture:true,castable:true,castCells:castCells(source),impactCells:[0,1,2].flatMap(i=>ringCells(landing,i)),targetCount:rows.reduce((n,r)=>n+r.stage.targets.length,0),castMessage:'4칸 투척 · 가운데가 빈 서리 고리 · 1→2→3칸'};
 };
 G.WT_SUPERCOOLED_MIXTURE={spec,validCell,living,inCastRange,castCells,clampAim,inRing,ringCells,targetsInRing,flightMs,createCast};
})(globalThis);
