/* Gem 59 / effect 51. Fixed two-cell forward censer slam, one lightning attack. */
(function(G){
 'use strict';
 const spec={slug:'ripple-judgment',name:'파문심판',gemNumber:59,effectNumber:51,category:'attack',element:'light',forwardCells:2,directions:[2,4,6,8],crossRadius:1,crossRadiusStatus:'working-default',hitsPerEnemy:1,balance:{damage:null,cooldown:null,cost:null},visualProposal:{impactMs:480,censerHoldMs:140,lightningMs:420}};
 const dirs={2:{dx:0,dy:1},4:{dx:-1,dy:0},6:{dx:1,dy:0},8:{dx:0,dy:-1}},cell=c=>({gx:c.gx,gy:c.gy}),same=(a,b)=>a.gx===b.gx&&a.gy===b.gy,validId=id=>typeof id==='string'?id.length>0:typeof id==='number'&&Number.isFinite(id),validCell=c=>!!(c&&Number.isInteger(c.gx)&&Number.isInteger(c.gy)&&c.gx>=0&&c.gx<9&&c.gy>=0&&c.gy<8);
 function landingCell(source,direction){if(!validCell(source)||!Object.hasOwn(dirs,direction))return null;const d=dirs[direction],c={gx:source.gx+d.dx*2,gy:source.gy+d.dy*2};return validCell(c)?c:null;}
 function crossCells(center,radius=1){if(![1,2].includes(radius))throw Error('Cross radius must be 1 or 2');if(!validCell(center))return[];const out=[cell(center)];for(const d of Object.values(dirs))for(let n=1;n<=radius;n++){const c={gx:center.gx+d.dx*n,gy:center.gy+d.dy*n};if(validCell(c))out.push(c);}return out;}
 const inCross=(center,target,radius=1)=>validCell(center)&&validCell(target)&&(target.gx===center.gx||target.gy===center.gy)&&Math.abs(target.gx-center.gx)+Math.abs(target.gy-center.gy)<=radius;
 function living(enemies){const ids=new Set();return(Array.isArray(enemies)?enemies:[]).filter(e=>{if(!validCell(e)||!validId(e.id)||ids.has(e.id)||e.alive===false||e.dead===true||e.isDead===true||e.hp!=null&&(!Number.isFinite(e.hp)||e.hp<=0))return false;ids.add(e.id);return true;});}
 const speedOf=n=>Number.isFinite(n)?Math.max(.5,Math.min(2.5,n)):1;
 // The censer remains tethered. Its physical bottom moves vertically during the last 30%.
 function pose(source,landing,direction,progress,out={}){const d=dirs[direction],hx=source.gx*48+24+d.dx*7,hy=source.gy*48+14+d.dy*3,tx=landing.gx*48+24,ty=landing.gy*48+24,p=Math.max(0,Math.min(1,progress)),lift=Math.min(66,Math.max(8,ty-18)),backX=hx-d.dx*16,backY=hy-d.dy*16-48;let x,y;
  if(p<.28){const u=p/.28;x=hx+d.dx*(14-30*u);y=hy+d.dy*(14-30*u)-30-18*u;}else if(p<.7){const u=(p-.28)/.42,v=u*u*(3-2*u);x=backX+(tx-backX)*v;y=backY+(ty-lift-backY)*v;}else{const u=(p-.7)/.3;x=tx;y=ty-lift*(1-u*u);}
  if(p===1){x=tx;y=ty;}Object.assign(out,{hx,hy,x,y});return out;
 }
 const reject=reason=>({ok:false,reason,done:true,impacts:0,update(){return 0;},cancel(){return false;}});
 function createCast({source,direction=6,crossRadius=spec.crossRadius,getEnemies,emit=()=>null,onImpact=()=>{},onCancel=()=>{},onVisualError=()=>{},startAt=0,speed=1,channelId='ripple-judgment'}={}){
  if(!validCell(source))return reject('invalid-source');if(!Object.hasOwn(dirs,direction))return reject('invalid-direction');const landing=landingCell(source,direction);if(!landing)return reject('two-cells-forward-outside-board');
  if(![1,2].includes(crossRadius)||[getEnemies,emit,onImpact,onCancel,onVisualError].some(f=>typeof f!=='function')||!Number.isFinite(startAt)||!validId(channelId))throw Error('Invalid ripple judgment cast');
  speed=speedOf(speed);const from=cell(source),impactMs=Math.round(480/speed),impactAt=startAt+impactMs,holdMs=Math.round(140/speed),lightningMs=Math.round(420/speed),endAt=impactAt+Math.max(holdMs,lightningMs),cells=crossCells(landing,crossRadius);
  if(!Number.isFinite(endAt)||impactAt<=startAt||endAt<=impactAt)throw Error('Invalid clock precision');
  let started=false,hit=false,cancelled=false,cancelledAt=null,updating=false,lastNow=-Infinity,lastVisualError=null;const handles=[];
  function visual(fn){try{return fn();}catch(e){lastVisualError=e;try{onVisualError(e);}catch(_){}return null;}}
  function send(phase,at,duration){const e={skillName:spec.name,kind:'stage',judgmentPhase:phase,at,duration,sourceCell:cell(from),targetCells:[cell(landing)],landingCell:cell(landing),judgmentDirection:direction,crossRadius,impactOffsetMs:impactMs,holdMs,footprint:{cells:cells.map(cell)},stageIndex:0,element:'light',damageCategory:'attack',channelId,visualOnly:true,renderLayer:'foreground'};handles.push(e);const h=visual(()=>emit(e));if(h&&typeof h==='object'&&h!==e)handles.push(h);if(cancelled)for(const h of handles)h.judgmentCancelledAt=cancelledAt;}
  const cast={ok:true,source:cell(from),landingCell:cell(landing),direction,crossRadius,startAt,impactAt,endAt,get done(){return cancelled||hit&&lastNow>=endAt;},get impacts(){return hit?1:0;},get cancelled(){return cancelled;},get lastVisualError(){return lastVisualError;},
   cancel(at=Number.isFinite(lastNow)?lastNow:startAt){if(cancelled||lastNow>=endAt)return false;if(!Number.isFinite(at))throw Error('Invalid cancellation');cancelled=true;cancelledAt=Math.max(startAt,lastNow,at);for(const h of handles)h.judgmentCancelledAt=cancelledAt;visual(()=>onCancel(channelId,cancelledAt));return true;},
   update(now){if(cancelled||updating||!Number.isFinite(now)||now<startAt||now<lastNow)return 0;lastNow=now;updating=true;try{
    if(!started){started=true;send('censer',startAt,impactMs+holdMs);}if(cancelled||hit||now<impactAt)return 0;
    const targets=living(getEnemies(impactAt)).filter(e=>inCross(landing,e,crossRadius));if(cancelled)return 0;hit=true;
    // The slam and cross are one attack batch. The centre never receives an accidental second hit.
    send('cross',impactAt,lightningMs);onImpact({at:impactAt,category:'attack',element:'light',damageType:'hit',damage:null,sourceCell:cell(from),landingCell:cell(landing),direction,crossRadius,cells:cells.map(cell),targets,targetIds:targets.map(e=>e.id),hitsPerEnemy:1,channelId});return 1;
   }finally{updating=false;}}
  };return cast;
 }
 function previewEnemies(center,source,mode,radius=1){if(!center||mode==='empty')return[];const all=mode==='single'?[cell(center)]:[...crossCells(center,radius),{gx:center.gx-1,gy:center.gy-1},{gx:center.gx+1,gy:center.gy-1},{gx:center.gx-1,gy:center.gy+1},{gx:center.gx+1,gy:center.gy+1}];return all.filter(c=>validCell(c)&&!same(c,source)).map((c,i)=>({id:'J'+i,...c,hp:100}));}
 if(!Array.isArray(G.WT_CATALOG))throw Error('Load catalog first');if(!G.WT_CATALOG.some(s=>s.id===51))G.WT_CATALOG.push({id:51,name:spec.name,slug:spec.slug,skill:{isGem:true,category:'attack',ele:'light',baseSpd:null,damage:null,desc:'향로로 플레이어 전방 2칸을 내려찍고 착지점에서 십자 모양 번개를 일으키는 번개 공격입니다. 적용 기본안: 상하좌우 방향, 중앙과 상하좌우 1칸의 적에게 각 1회 타격합니다.',tags:['attack','light','area'],targetMode:'forward-two-cross',combatPattern:{kind:'rippleJudgment',forwardCells:2,crossRadius:1}},grid:{kind:'area',range:2},previewDefaults:{speed:1},profile:{family:'rippleJudgment',scale:1}});
 const previous=G.buildWTPreview;G.buildWTPreview=function(item,options={}){if(item.id!==51)return previous(item,options);const source=options.source||{gx:2,gy:4},direction=options.judgmentDirection??6,radius=options.crossRadius??spec.crossRadius,landing=landingCell(source,direction),enemies=options.enemies||previewEnemies(landing,source,options.judgmentMode,radius),events=[],rows=[],timeline=[{at:0,label:landing?'향로 들어 올리기':'전방 2칸이 보드 밖입니다',element:'light'}];const cast=createCast({source,direction,crossRadius:radius,speed:options.attackSpeed,getEnemies:()=>enemies,emit:e=>{events.push(e);return e;},onImpact:h=>{rows.push({index:0,at:h.at,from:cell(source),impact:h.cells,footprint:{cells:h.cells},element:'light',stage:{kind:'rippleJudgment',label:'향로 내려찍기 · 십자 번개',damageMultiplier:null,targets:h.targets.map(enemy=>({enemy}))}});timeline.push({at:h.at,label:'전방 2칸 착지 · 십자 번개 '+h.targets.length+'마리',element:'light'});}});if(cast.ok){cast.update(0);cast.update(cast.impactAt);}return{events,rows,timeline,duration:cast.ok?cast.endAt+180:1000,source:cell(source),target:landing||cell(source),landingCell:landing,direction,crossRadius:radius,castable:cast.ok,impactAt:cast.impactAt??null,targets:living(enemies).map(enemy=>({enemy})),profile:{kind:'area',range:2},attackSpeed:speedOf(options.attackSpeed),channelInterval:null,motion:0,rippleJudgment:true,castCells:landing?crossCells(landing,radius):[],targetCount:rows[0]?.stage.targets.length??0,castMessage:cast.ok?'번개 공격 · 전방 2칸 · 십자 범위 각 1회':'전방 2칸이 보드 밖이므로 시전하지 않습니다'};};
 G.WT_RIPPLE_JUDGMENT={spec,dirs,validCell,living,landingCell,crossCells,inCross,createCast,previewEnemies,pose};
})(globalThis);
