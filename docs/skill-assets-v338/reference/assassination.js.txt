/* Gem 60 / effect 52. Movement is committed by the host; one physical dagger hit follows. */
(function(G){
 'use strict';
 const spec={slug:'assassination',name:'암살',gemNumber:60,effectNumber:52,category:'attack',element:'phys',range:4,distanceMetric:'chebyshev',targetLimit:1,weapon:'dagger',behindRule:'one-cell-opposite-target-facing',blockedRule:'fail-without-front-or-side-fallback',ailment:{mode:'random-one',chancePercent:30,choices:['poison','bleed'],status:'working-default',durationMs:null,damage:null},balance:{damage:null,cooldown:null,cost:null},visualProposal:{teleportMs:200,hitMs:340,slashMs:190,confirmationMs:700}};
 const dirs={2:{x:0,y:1},4:{x:-1,y:0},6:{x:1,y:0},8:{x:0,y:-1}},validCell=c=>!!(c&&Number.isInteger(c.gx)&&Number.isInteger(c.gy)&&c.gx>=0&&c.gx<9&&c.gy>=0&&c.gy<8),validId=id=>typeof id==='string'?id.length>0:typeof id==='number'&&Number.isFinite(id),cell=c=>({gx:c.gx,gy:c.gy}),same=(a,b)=>a.gx===b.gx&&a.gy===b.gy,dist=(a,b)=>Math.max(Math.abs(a.gx-b.gx),Math.abs(a.gy-b.gy)),speedOf=n=>Number.isFinite(n)?Math.max(.5,Math.min(2.5,n)):1;
 function living(enemies){const ids=new Set();return(Array.isArray(enemies)?enemies:[]).filter(e=>{if(!validCell(e)||!validId(e.id)||ids.has(e.id)||e.alive===false||e.dead===true||e.isDead===true||e.hp!=null&&(!Number.isFinite(e.hp)||e.hp<=0))return false;ids.add(e.id);return true;});}
 function facing(target,source,explicit){if(dirs[explicit])return +explicit;if(dirs[target.facingDirection??target.direction])return +(target.facingDirection??target.direction);const x=source.gx-target.gx,y=source.gy-target.gy;return Math.abs(x)>=Math.abs(y)?x<0?4:6:y<0?8:2;}
 function behind(target,direction){const d=dirs[direction];if(!validCell(target)||!d)return null;const c={gx:target.gx-d.x,gy:target.gy-d.y};return validCell(c)?c:null;}
 function plan({source,target,range=4,enemies=[],getFacing,isWalkable=()=>true,occupiedCells=[]}){
  if(!validCell(source)||!living([target]).length)return {ok:false,reason:'대상 없음'};
  if(!Number.isFinite(range)||range<1||range>8||dist(source,target)>range)return {ok:false,reason:'사거리 밖'};
  if(same(source,target))return {ok:false,reason:'플레이어와 대상 칸 중복'};
  const direction=facing(target,source,getFacing?.(target)),destination=behind(target,direction);
  if(!destination)return {ok:false,reason:'적 뒤쪽 칸이 보드 밖'};
  if(!isWalkable(destination)||living(enemies).some(e=>e.id!==target.id&&same(e,destination))||(Array.isArray(occupiedCells)?occupiedCells:[]).some(c=>validCell(c)&&same(c,destination)&&!same(c,source)))return {ok:false,reason:'적 뒤쪽 칸이 막힘'};
  return {ok:true,targetId:target.id,targetCell:cell(target),sourceCell:cell(source),destination,direction,playerDirection:direction};
 }
 function rollAilment({mode='random-one',chancePercent=30,selected='poison',rng=Math.random}={}){
  if(!['random-one','independent','selected'].includes(mode)||!['poison','bleed'].includes(selected)||!Number.isFinite(chancePercent)||chancePercent<0||chancePercent>100||typeof rng!=='function')throw Error('Invalid ailment configuration');
  const random=()=>{const n=rng();if(!Number.isFinite(n)||n<0||n>=1)throw Error('RNG must return [0,1)');return n;};
  const candidates=mode==='random-one'?[random()<.5?'poison':'bleed']:mode==='selected'?[selected]:['poison','bleed'];return candidates.map(type=>({type,chancePercent,success:chancePercent===100||chancePercent>0&&random()<chancePercent/100}));
 }
 function createCast({source,targetId,getEnemies,getSourceCell,getTargetFacing,isWalkable=()=>true,getOccupiedCells=()=>[],onTeleport,onHit,onAilment=()=>{},emit=()=>{},onVisualError=()=>{},startAt=0,speed=1,range=4,ailmentMode=spec.ailment.mode,chancePercent=spec.ailment.chancePercent,selectedAilment='poison',rng=Math.random,channelId='assassination'}={}){
  if(!validCell(source)||!validId(targetId)||typeof getEnemies!=='function'||typeof onTeleport!=='function'||typeof onHit!=='function'||!Number.isFinite(startAt)||!['random-one','independent','selected'].includes(ailmentMode)||!Number.isFinite(chancePercent)||chancePercent<0||chancePercent>100||!['poison','bleed'].includes(selectedAilment))throw Error('Invalid assassination host binding');
  const from=cell(source),rate=speedOf(speed),teleportAt=startAt+Math.round(200/rate),hitAt=startAt+Math.round(340/rate),slashMs=Math.round(190/rate),confirmationMs=700,handles=[];
  let currentCell=cell(source),started=false,teleported=false,resolved=false,cancelled=false,lastNow=-Infinity,updating=false,locked=null,statusRolls=[],lastError=null;
  const inspect=at=>{const enemies=living(getEnemies(at)),target=enemies.find(e=>e.id===targetId),where=getSourceCell?getSourceCell(at):currentCell;return {...plan({source:where,target,range,enemies,getFacing:getTargetFacing,isWalkable,occupiedCells:getOccupiedCells(at)}),target};};
  const initial=inspect(startAt),send=(phase,at,duration,extra={})=>{const e={skillName:spec.name,kind:phase==='confirmation'?'hit':phase==='vanish'?'windup':'stage',assassinationPhase:phase,at,duration,sourceCell:cell(from),targetCells:[cell(locked?.targetCell||initial.targetCell)],stageIndex:0,element:'phys',category:'attack',channelId,visualOnly:true,...extra};handles.push(e);try{const h=emit(e);if(h&&typeof h==='object'&&h!==e)handles.push(h);}catch(error){try{onVisualError(error);}catch(_){}}};
  const fail=reason=>{cast.reason=reason;resolved=true;};
  const cast={ok:initial.ok,reason:initial.reason||'',initialPlan:initial.ok?{...initial,target:undefined}:null,teleportAt,hitAt,endAt:hitAt+Math.max(slashMs,confirmationMs),get teleported(){return teleported;},get resolved(){return resolved;},get cancelled(){return cancelled;},get lockedPlan(){return locked?{...locked,destination:cell(locked.destination),targetCell:cell(locked.targetCell)}:null;},get rolls(){return statusRolls.map(r=>({...r}));},get lastError(){return lastError;},
   cancel(at=lastNow){if(!Number.isFinite(at))at=startAt;if(at<lastNow)at=lastNow;cancelled=true;for(const h of handles)h.assassinationCancelledAt=at;},
   update(now){if(!Number.isFinite(now)||now<startAt||now<lastNow||updating||cancelled||!cast.ok||resolved)return 0;lastNow=now;updating=true;try{
    if(!started){started=true;send('vanish',startAt,teleportAt-startAt+80,{blinkCell:cell(from)});}
    if(cancelled)return 0;
    if(!teleported&&now>=teleportAt){const next=inspect(teleportAt);if(cancelled)return 0;if(!next.ok){fail(next.reason);return 0;}locked={targetId,sourceCell:cell(next.sourceCell),targetCell:cell(next.targetCell),destination:cell(next.destination),direction:next.direction};
     // The callback must atomically verify occupancy and return true after moving the actor.
     const committed=onTeleport({at:teleportAt,from:cell(next.sourceCell),to:cell(next.destination),targetId,targetFacing:next.direction,playerFacing:next.direction});if(committed!==true){fail('순간이동이 완료되지 않음');return 0;}teleported=true;currentCell=cell(next.destination);if(cancelled)return 0;send('arrive',teleportAt,180,{blinkCell:cell(currentCell)});send('dagger',teleportAt,hitAt-teleportAt+60,{landingCell:cell(currentCell),targetCells:[cell(next.targetCell)],facingDirection:next.direction});
    }
    if(cancelled||!teleported||now<hitAt)return 0;resolved=true;
    const target=living(getEnemies(hitAt)).find(e=>e.id===targetId),where=getSourceCell?getSourceCell(hitAt):currentCell;
    if(!target||!same(target,locked.targetCell)||!validCell(where)||!same(where,locked.destination)){cast.reason='대상 또는 플레이어 이동으로 빗나감';return 0;}
    if(cancelled)return 0;send('slash',hitAt,slashMs,{landingCell:cell(where),targetCells:[cell(target)],facingDirection:locked.direction});
    const hit={at:hitAt,category:'attack',element:'phys',damageType:'hit',weapon:'dagger',damage:null,target,targetId,targets:[target],targetIds:[targetId],sourceCell:cell(where),cells:[cell(target)],hitsPerEnemy:1,channelId};const hitResult=onHit(hit);if(cancelled||hitResult===false){cast.reason='적중 취소';return 0;}
    const survivor=living(getEnemies(hitAt)).find(e=>e.id===targetId);if(!survivor)return 1;
    statusRolls=rollAilment({mode:ailmentMode,chancePercent,selected:selectedAilment,rng});for(const roll of statusRolls)if(roll.success&&!cancelled){const accepted=onAilment({...roll,at:hitAt,target:survivor,targetId,sourceCell:cell(where),durationMs:null,damage:null,channelId});if(accepted!==false&&!cancelled)send('confirmation',hitAt,confirmationMs,{targetCells:[cell(survivor)],ailmentType:roll.type,confirmationOnly:true});}return 1;
   }catch(error){lastError=error;fail('호스트 처리 오류');throw error;}finally{updating=false;}}
  };return cast;
 }
 const seeded=seed=>{let state=seed>>>0;return()=>{state=(Math.imul(state,1664525)+1013904223)>>>0;return state/4294967296;};};
 function actorPose(seq,at){const cancelledAt=seq.assassinationCancelledAt??Infinity,moved=seq.teleported&&Math.min(at,cancelledAt)>=seq.teleportAt,c=moved?seq.landingCell:seq.source;let alpha=1;if(!seq.castable||at>=cancelledAt)return {cell:c,alpha};if(!moved&&at>=seq.teleportAt-100)alpha=Math.max(0,(seq.teleportAt-at)/100);else if(moved)alpha=Math.min(1,(at-seq.teleportAt)/70);return {cell:c,alpha};}
 if(!G.WT_CATALOG.some(s=>s.id===52))G.WT_CATALOG.push({id:52,name:spec.name,slug:spec.slug,skill:{isGem:true,ele:'phys',category:'attack',baseSpd:null,damage:null,targetMode:'behind-target',targets:1,desc:'적이 바라보는 방향의 뒤쪽으로 순간이동해 단검으로 물리 피해를 줍니다. 적중 시 중독 또는 출혈 중 하나를 선택해 확률 판정합니다. 기본안: 사거리 4칸·상태 이상 확률 30%, 뒤쪽 칸이 막히면 시전 불가.',tags:['attack','melee','physical','mobility','dagger'],combatPattern:{kind:'assassination',range:4}},grid:{kind:'melee',range:4},previewDefaults:{speed:1},profile:{family:'assassination',scale:1}});
 const previous=G.buildWTPreview;G.buildWTPreview=function(item,options={}){if(item.id!==52)return previous(item,options);const source=options.source||{gx:2,gy:4},target=options.assassinTarget||options.aim||{gx:5,gy:3},direction=options.assassinDirection??4,mode=options.assassinScene||'open',enemies=options.enemies||[{id:'A',...cell(target),hp:100,facingDirection:direction}];let actor=cell(source);const rear=behind(target,direction);if(mode==='occupied'&&rear)enemies.push({id:'block',...rear,hp:100,facingDirection:2});const events=[],rows=[],timeline=[{at:0,label:'단검 준비',element:'phys'}];
  const cast=createCast({source,targetId:enemies[0]?.id||'missing',getEnemies:()=>mode==='empty'?[]:enemies,getSourceCell:()=>actor,isWalkable:c=>mode!=='wall'||!rear||!same(c,rear),speed:options.attackSpeed,chancePercent:options.assassinChance??30,ailmentMode:options.assassinAilmentMode??spec.ailment.mode,selectedAilment:options.assassinSelected??'poison',rng:seeded(options.seed??44),emit:e=>{events.push(e);return e;},onTeleport:h=>{actor=cell(h.to);timeline.push({at:h.at,label:'적 뒤로 순간이동',element:'phys'});return true;},onHit:h=>{rows.push({index:0,at:h.at,from:cell(actor),impact:h.cells,footprint:{cells:h.cells},element:'phys',stage:{kind:'assassination',label:'단검 베기 · 물리 1회',damageMultiplier:null,targets:h.targets.map(enemy=>({enemy}))}});timeline.push({at:h.at,label:'단검 물리 타격',element:'phys'});return true;},onAilment:h=>{timeline.push({at:h.at,label:h.type==='poison'?'중독 성공':'출혈 성공',element:h.type==='poison'?'chaos':'phys'});return true;}});if(cast.ok){cast.update(0);cast.update(cast.hitAt);}
  return {events,rows,timeline,duration:cast.ok?cast.endAt+200:1000,source:cell(source),target:cell(target),landingCell:cast.lockedPlan?.destination||cast.initialPlan?.destination||null,teleportAt:cast.teleportAt,impactAt:cast.hitAt,teleported:cast.teleported,targetFacing:direction,assassination:true,castable:cast.ok,castReason:cast.reason,ailmentRolls:cast.rolls,targets:enemies.map(enemy=>({enemy})),targetCount:rows.length,profile:{kind:'melee',range:4},attackSpeed:speedOf(options.attackSpeed),channelInterval:null,motion:cast.teleportAt,castMessage:cast.ok?'적 뒤로 순간이동 → 단검 물리 타격'+(cast.rolls.some(r=>r.success)?' → '+cast.rolls.filter(r=>r.success).map(r=>r.type==='poison'?'중독':'출혈').join('·'):' · 상태 이상 미발동'):cast.reason};
 };
 G.WT_ASSASSINATION={spec,dirs,validCell,living,facing,behind,plan,rollAilment,createCast,seeded,actorPose};
})(globalThis);
