/* Gem 57 / effect 49. Physical projectile attack; host owns numeric damage. */
(function(G){
 'use strict';
 const spec={slug:'empty-flask',name:'빈 플라스크',gemNumber:57,effectNumber:49,category:'attack',element:'phys',range:4,shardRange:3,distanceMetric:'euclidean',baseShards:{min:1,max:4,selection:'uniform-per-cast'},extraProjectileChance:{unit:'percent',min:0,max:100,rule:'one-roll-per-base-shard',recursive:false},maxShards:8,targetRule:'other-monsters-first-then-repeat',homing:false,balance:{damage:null,shardDamage:null,cooldown:null,cost:null},visualProposal:{flightBaseMs:420,flightMsPerCell:45,shatterMs:200,shardDelayMs:70,shardStaggerMs:26,shardBaseMs:150,shardMsPerCell:46,contactMs:110}};
 const validCell=c=>!!(c&&Number.isInteger(c.gx)&&Number.isInteger(c.gy)&&c.gx>=0&&c.gx<9&&c.gy>=0&&c.gy<8),validId=id=>typeof id==='string'?id.length>0:typeof id==='number'&&Number.isFinite(id),cell=c=>({gx:c.gx,gy:c.gy}),sameCell=(a,b)=>a.gx===b.gx&&a.gy===b.gy,d2=(a,b)=>(a.gx-b.gx)**2+(a.gy-b.gy)**2;
 const inRange=(a,b,r)=>validCell(a)&&validCell(b)&&d2(a,b)<=r*r,inCastRange=(a,b)=>inRange(a,b,4),inShardRange=(a,b)=>inRange(a,b,3);
 function cellsWithin(c,r){const a=[];if(validCell(c))for(let gy=0;gy<8;gy++)for(let gx=0;gx<9;gx++)if(inRange(c,{gx,gy},r))a.push({gx,gy});return a;}
 function clampAim(source,aim){if(!validCell(source)||!aim||!Number.isFinite(aim.gx)||!Number.isFinite(aim.gy))return null;if(inCastRange(source,aim))return cell(aim);const target={gx:Math.max(-1e6,Math.min(1e6,aim.gx)),gy:Math.max(-1e6,Math.min(1e6,aim.gy))};let best=null,dist=Infinity;for(const c of cellsWithin(source,4)){const n=d2(c,target);if(n<dist){best=c;dist=n;}}return best;}
 function living(enemies){if(!Array.isArray(enemies))return[];const ids=new Set();return enemies.filter(e=>{if(!validCell(e)||!validId(e.id)||ids.has(e.id)||e.alive===false||e.dead===true||e.isDead===true||e.hp!=null&&(!Number.isFinite(e.hp)||e.hp<=0))return false;ids.add(e.id);return true;});}
 const chance=n=>Number.isFinite(n)?Math.max(0,Math.min(100,n)):0,visualSpeed=n=>Number.isFinite(n)?Math.max(.5,Math.min(2.5,n)):1;
 function randomUnit(rng){const n=rng();return Number.isFinite(n)?Math.max(0,Math.min(.999999999999,n)):0;}
 function seeded(seed){let n=(Number.isFinite(seed)?seed:49)>>>0;return()=>((n=(Math.imul(n,1664525)+1013904223)>>>0)/4294967296);}
 function rollShards({baseCount=null,extraProjectileChance=0,rng=Math.random}={}){if(typeof rng!=='function')throw Error('Invalid RNG');if(baseCount!=null&&(!Number.isInteger(baseCount)||baseCount<1||baseCount>4))throw Error('Base shards must be 1..4');const base=baseCount??(1+Math.floor(randomUnit(rng)*4)),percent=chance(extraProjectileChance),shards=[];for(let i=0;i<base;i++){shards.push({baseIndex:i,bonus:false});if(percent===100||percent>0&&randomUnit(rng)<percent/100)shards.push({baseIndex:i,bonus:true});}return{baseCount:base,extraCount:shards.length-base,totalShards:shards.length,chancePercent:percent,shards};}
 function chooseTargets(center,enemies,count,primaryId,rng=Math.random){if(typeof rng!=='function')throw Error('Invalid RNG');count=Number.isInteger(count)?Math.max(0,Math.min(spec.maxShards,count)):0;const live=living(enemies).filter(e=>inShardRange(center,e)),others=live.filter(e=>e.id!==primaryId),unused=[...others],result=[];for(let i=0;i<count;i++){const pool=unused.length?unused:others.length?others:live;if(!pool.length)break;const n=Math.floor(randomUnit(rng)*pool.length),e=pool[n];result.push({id:e.id,...cell(e)});if(pool===unused)unused.splice(n,1);}return result;}
 const flightMs=(a,b,speed=1)=>Math.round((420+Math.sqrt(d2(a,b))*45)/visualSpeed(speed)),shardMs=(a,b,speed=1)=>Math.round((150+Math.sqrt(d2(a,b))*46)/visualSpeed(speed));
 const reject=reason=>({ok:false,reason,done:true,impacts:0,update(){return 0;},cancel(){return false;}});
 function createCast({source,aim,primaryTargetId=null,getEnemies,emit=()=>null,onImpact=()=>{},onShatter=()=>{},onCancel=()=>{},onVisualError=()=>{},rng=Math.random,baseCount=null,extraProjectileChance=0,startAt=0,speed=1,channelId='empty-flask'}={}){
  if(!validCell(source))return reject('invalid-source');const landing=clampAim(source,aim);if(!landing)return reject('invalid-aim');if([getEnemies,emit,onImpact,onShatter,onCancel,onVisualError,rng].some(f=>typeof f!=='function')||!Number.isFinite(startAt)||!validId(channelId)||primaryTargetId!=null&&!validId(primaryTargetId))throw Error('Invalid empty flask cast');
  speed=visualSpeed(speed);const from=cell(source),flightDuration=flightMs(from,landing,speed),impactAt=startAt+flightDuration,plan=rollShards({baseCount,extraProjectileChance,rng}),shatterDuration=Math.round(200/speed),contactDuration=Math.round(110/speed);let endAt=impactAt+shatterDuration;
  if(!Number.isFinite(endAt)||impactAt<=startAt||endAt<=impactAt)throw Error('Invalid clock precision');
  let started=false,landed=false,launched=false,cancelled=false,cancelledAt=null,updating=false,lastNow=-Infinity,currentAt=null,primaryId=primaryTargetId,processed=0,index=0,lastVisualError=null;const handles=[],pending=[];
  function visual(fn){try{return fn();}catch(error){lastVisualError=error;try{onVisualError(error);}catch(_){}return null;}}
  function send(phase,at,duration,origin,target,extra={}){const event={skillName:spec.name,kind:phase==='flask'||phase==='shard'?'travel':phase==='contact'?'hit':'stage',emptyFlaskPhase:phase,at,duration,sourceCell:cell(origin),targetCells:[cell(target)],landingCell:cell(landing),stageIndex:0,element:'phys',damageCategory:'attack',channelId,visualOnly:true,renderLayer:'foreground',...extra};handles.push(event);const h=visual(()=>emit(event));if(h&&typeof h==='object'&&h!==event)handles.push(h);if(cancelled)for(const h of handles)h.emptyFlaskCancelledAt=cancelledAt;}
  function hit(phase,at,target,p={}){const targets=target?[target]:[];onImpact({phase,at,index:processed-1,category:'attack',element:'phys',damageType:'hit',isHit:targets.length>0,damage:null,sourceCell:cell(from),landingCell:cell(landing),targets,targetIds:targets.map(e=>e.id),cells:targets.map(cell),canTriggerAdditionalProjectiles:false,channelId,...p});}
  const cast={ok:true,source:cell(from),landingCell:cell(landing),startAt,impactAt,flightDuration,plan:{...plan,shards:plan.shards.map(s=>({...s}))},get endAt(){return endAt;},get done(){return cancelled||launched&&index===pending.length;},get impacts(){return processed;},get shardTargets(){return pending.map(p=>({...p,cell:cell(p.cell)}));},get cancelled(){return cancelled;},get lastVisualError(){return lastVisualError;},
   cancel(at=currentAt??(Number.isFinite(lastNow)?lastNow:startAt)){if(cancelled||launched&&index===pending.length&&lastNow>=endAt)return false;if(!Number.isFinite(at))throw Error('Invalid cancellation');cancelled=true;cancelledAt=Math.max(startAt,currentAt??(Number.isFinite(lastNow)?lastNow:startAt),at);for(const h of handles)h.emptyFlaskCancelledAt=cancelledAt;visual(()=>onCancel(channelId,cancelledAt));return true;},
   update(now){if(cancelled||updating||!Number.isFinite(now)||now<startAt||now<lastNow)return 0;lastNow=now;updating=true;let n=0;try{
    if(!started){started=true;send('flask',startAt,flightDuration,from,landing);}if(cancelled||now<impactAt)return 0;
    if(!landed){currentAt=impactAt;const live=living(getEnemies(impactAt)),target=live.find(e=>sameCell(e,landing)&&(primaryId==null||e.id===primaryId));if(cancelled)return 0;primaryId=target?.id??primaryId;landed=true;processed++;n++;send('shatter',impactAt,shatterDuration,landing,landing);if(cancelled)return n;hit('flask',impactAt,target);currentAt=null;}
    if(!launched&&!cancelled){currentAt=impactAt;const targets=chooseTargets(landing,getEnemies(impactAt),plan.totalShards,primaryId,rng);if(cancelled)return n;launched=true;
     for(let i=0;i<plan.totalShards;i++){const descriptor=plan.shards[i],target=targets[i],at=impactAt+Math.round((70+i*26)/speed),fallback=cellsWithin(landing,3).filter(c=>!sameCell(c,landing)),end=target?cell(target):fallback[Math.floor(i*fallback.length/plan.totalShards)]||cell(landing),duration=shardMs(landing,end,speed),arrivesAt=at+duration;
      if(!Number.isFinite(arrivesAt+contactDuration)||at<=impactAt||arrivesAt<=at)throw Error('Invalid shard clock precision');const p={id:target?.id??null,cell:end,at,arrivesAt,duration,projectileIndex:i,...descriptor};pending.push(p);endAt=Math.max(endAt,arrivesAt+contactDuration);send('shard',at,duration,landing,end,{projectileIndex:i,bonusShard:descriptor.bonus,shardTargetId:p.id});if(cancelled)break;
     }pending.sort((a,b)=>a.arrivesAt-b.arrivesAt||a.projectileIndex-b.projectileIndex);if(!cancelled)onShatter({at:impactAt,...cast.plan,targetCount:targets.length,channelId});currentAt=null;
    }
    while(index<pending.length&&pending[index].arrivesAt<=now&&!cancelled){const p=pending[index];currentAt=p.arrivesAt;const target=p.id==null?null:living(getEnemies(p.arrivesAt)).find(e=>e.id===p.id&&sameCell(e,p.cell));if(cancelled)break;index++;processed++;n++;if(target)send('contact',p.arrivesAt,contactDuration,landing,p.cell,{projectileIndex:p.projectileIndex,bonusShard:p.bonus});if(cancelled)break;hit('shard',p.arrivesAt,target,{projectileIndex:p.projectileIndex,baseIndex:p.baseIndex,bonusShard:p.bonus,aimCell:cell(p.cell)});currentAt=null;
    }return n;
   }finally{currentAt=null;updating=false;}}
  };return cast;
 }
 if(!Array.isArray(G.WT_CATALOG))throw Error('Load catalog first');if(!G.WT_CATALOG.some(s=>s.id===49))G.WT_CATALOG.push({id:49,name:spec.name,slug:spec.slug,skill:{isGem:true,category:'attack',ele:'phys',baseSpd:null,damage:null,desc:'빈 유리병을 던져 물리 피해를 가합니다. 깨진 병에서 기본 1~4개의 유리 파편이 나와 주변 3칸의 몬스터를 추가 타격합니다. 기본 파편마다 투사체 추가 발사 확률을 한 번 판정해 파편이 더 생성됩니다. 다른 적부터 분산하고, 대상이 부족하면 같은 적을 다시 노립니다.',tags:['attack','projectile','phys'],targetMode:'flask-then-shards',combatPattern:{kind:'emptyFlask',baseShards:[1,4],extraProjectileChance:true}},grid:{kind:'chain',range:4},previewDefaults:{speed:1},profile:{family:'emptyFlask',scale:1}});
 function previewEnemies(center,source,mode){if(mode==='empty')return[];const offsets=mode==='single'?[[0,0]]:[[0,0],[1,-1],[2,0],[-2,1],[0,3],[-1,-2],[3,3]];return offsets.map(([dx,dy],i)=>({id:'G'+i,gx:center.gx+dx,gy:center.gy+dy,hp:100})).filter(e=>validCell(e)&&!sameCell(e,source));}
 const previous=G.buildWTPreview;G.buildWTPreview=function(item,options={}){if(item.id!==49)return previous(item,options);const source=options.source||{gx:1,gy:4},aim=options.aim||{gx:4,gy:4},landing=clampAim(source,aim);if(!landing)throw Error('Invalid empty flask preview');const enemies=options.enemies||previewEnemies(landing,source,options.emptyMode),events=[],rows=[],timeline=[{at:0,stage:0,label:'빈 플라스크 투척',element:'phys'}],speed=visualSpeed(options.attackSpeed),cast=createCast({source,aim,getEnemies:()=>enemies,rng:seeded(options.seed??49),speed,baseCount:options.emptyBaseCount??null,extraProjectileChance:options.emptyExtraChance??50,emit:e=>{events.push(e);return e;},onImpact:h=>{const label=h.phase==='flask'?'빈 병 적중':(h.bonusShard?'추가 ':'')+'유리 파편 '+(h.projectileIndex+1),footprint={cells:h.cells};rows.push({index:rows.length,at:h.at,from:h.phase==='flask'?cell(source):cell(landing),impact:h.cells,footprint,element:'phys',stage:{kind:h.phase,label:label+(h.isHit?'':' · 빗나감'),damageMultiplier:null,targets:h.targets.map(enemy=>({enemy}))}});timeline.push({at:h.at,label:label+(h.isHit?' · '+h.targetIds.join(','):' · 대상 없음'),element:'phys'});}});cast.update(0);cast.update(1e5);timeline.sort((a,b)=>a.at-b.at);return{events,rows,timeline,duration:cast.endAt+320,source:cell(source),target:cell(landing),landingCell:cell(landing),impactAt:cast.impactAt,flightDuration:cast.flightDuration,targets:living(enemies).map(enemy=>({enemy})),profile:{kind:'chain',range:4},attackSpeed:speed,channelInterval:null,motion:0,emptyFlask:true,shardPlan:cast.plan,shardTargets:cast.shardTargets,castCells:cellsWithin(source,4),shardCells:cellsWithin(landing,3),targetCount:rows.reduce((n,r)=>n+r.stage.targets.length,0),castMessage:'물리 공격 · 유리 파편 '+cast.plan.baseCount+' + '+cast.plan.extraCount+'개'};};
 G.WT_EMPTY_FLASK={spec,validCell,living,inCastRange,inShardRange,clampAim,cellsWithin,rollShards,chooseTargets,flightMs,shardMs,seeded,createCast};
})(globalThis);
