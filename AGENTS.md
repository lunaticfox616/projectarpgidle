# Project ARPG Idle — 프로젝트 헌법

이 문서는 이 저장소에서 작업하는 모든 사람과 AI Agent가 항상 지켜야 하는 상위 규칙이다.
세부 실행 절차는 저장소의 `.agents/skills/ponytail/SKILL.md`가 담당한다. `MUST`와 `금지`는
예외 없는 요구사항이다. 규칙이 충돌하면 이 문서, 사용자 요구, Ponytail 순으로 적용한다.

## 1. 필수 실행 절차: Ponytail

코드 작성·수정·삭제, 리팩터링, 설계, 코드 리뷰, 버그 진단·수정, 의존성 선택에는 반드시
`$ponytail` 스킬을 사용한다. 기본 강도는 `full`이다. 사용자가 명시적으로 `stop ponytail` 또는
`normal mode`라고 한 해당 작업만 예외다.

Ponytail은 다음 행동을 강제하는 실행 절차다.

- 먼저 실제 흐름과 모든 호출부를 이해하고, 증상이 아니라 공통 원인을 고친다.
- 새 코드를 만들기 전에 저장소의 기존 구현, 표준 기능, 이미 설치된 의존성을 차례로 찾는다.
- 요구사항을 만족하는 가장 작은 응집된 변경을 선택하고 추측성 확장은 만들지 않는다.
- 비자명한 로직에는 그 로직이 깨지면 실패하는 최소 한 개의 실행 가능한 검사를 남긴다.

단, 단순함을 이유로 입력 검증, 데이터 손실 방지, 오류 처리, 보안, 접근성, 저장 호환성,
명시된 요구사항을 생략해서는 안 된다. Ponytail은 아래의 아키텍처와 품질 계약을 완화하지 않는다.

## 2. 런타임과 저장소 지도

이 애플리케이션은 빌드 단계와 ES module이 없는 브라우저 JavaScript 게임이다. `index.html`의
`<script>` 순서와 명시적으로 노출된 브라우저 전역이 런타임 계약이다. import/export 기반
프로젝트처럼 가정하지 않는다.

| 경로 | 책임 |
| --- | --- |
| `data/` | 게임 상수, 정적 데이터, 콘텐츠 정의 |
| `js/utils.js` | 도메인 비종속 유틸리티와 안전한 전역 노출 도구 |
| `js/state.js` | 런타임 상태의 기본 shape, 초기화, 불변식 |
| `js/save.js` | 직렬화, 복원, 마이그레이션 경계 |
| `js/items.js`, `js/skills.js`, `js/passives.js`, `js/core-cube.js` | 각 게임 도메인의 규칙 |
| `js/combat-grid.js` | 8x8 전장 좌표, 범위, 이동, 스폰 배치 |
| `js/combat.js` | 전투와 진행 규칙 |
| `js/canvas-*.js` | 읽기 전용 도메인 결과의 캔버스 표현 |
| `js/ui.js`, `js/*-ui.js`, `js/cosmos-atlas.js` | DOM 조립과 사용자 이벤트 경계 |
| `js/main.js` | 최종 부트스트랩과 최상위 오케스트레이션 |
| `scripts/` | Node 기반 회귀·스모크 검사; 프로덕션에서 로드하지 않음 |

허용하는 기본 의존 방향은 다음과 같다.

```text
data -> utils -> state/save -> domain(items/skills/passives/core-cube)
     -> combat -> rendering/UI -> main
```

오른쪽 계층은 왼쪽을 사용할 수 있지만 반대 방향 의존은 금지한다. 도메인은 UI 전용 함수나 DOM에
의존하지 않는다. 양방향 호출과 이벤트·콜백으로 숨긴 순환 의존도 금지한다. 공통 shape는 가장 낮은
공통 계층이 소유한다: 정적 정의는 `data/`, 런타임 상태는 `js/state.js`, 도메인 비종속 변환은
`js/utils.js`가 단일 출처다.

`index.html`의 스크립트 순서를 바꾸거나 새 소비자를 추가하면 생산자가 먼저 로드되는지 확인하고,
저장 로드부터 첫 렌더까지 실제 브라우저에서 검증한다.

## 3. 작업 계약

### 탐색과 설계

수정 전에 대상 심볼과 유사 용어를 `rg`로 검색한다. 생산자뿐 아니라 모든 호출부,
`safeExposeGlobals`/`window` 노출, 관련 `scripts/smoke-*.js`, `index.html` 로드 순서를 확인한다.
검색 없이 편의를 위한 `format*`, `normalize*`, `safe*`, `ensure*`, `get*` helper나 새 shape를 만들지
않는다. 이름이 달라도 같은 개념이면 중복이다.

비자명한 변경은 구현 전에 변경할 행동과 유지할 행동, 소유 모듈, 입력·출력·상태 변경, 실패 방식,
경계값과 손상된 저장 데이터, 의존 방향, 사용자가 관찰할 검증 결과를 짧게 정한다. 질문 없이 안전하게
확인할 수 있는 내용은 코드와 테스트에서 먼저 확인한다.

### 구현

- 기존 구현을 대체할 자리에는 그 구현을 수정하거나 제거한다. 병렬 구현, 죽은 분기, 주석 처리 코드,
  제거 조건 없는 임시 호환 계층을 남기지 않는다.
- 계산과 상태 변경을 분리하고, 함수는 한 책임과 한 추상화 수준을 갖게 한다.
- 기존의 무관한 사용자 변경과 더러운 작업트리를 보존한다. 범위 밖 파일을 포맷하거나 되돌리지 않는다.
- 새 전역은 금지한다. 레거시 인라인 handler 때문에 불가피하면 소유 모듈에서
  `safeExposeGlobals({ name })`를 사용하고, 이름 충돌·모든 소비자·로드 순서를 검사한다.
- 저장 shape 변경에는 기본값, 이전 저장 마이그레이션, 손상 데이터 처리, 중복 적용 방지가 함께 있어야
  한다. 단위(ms/초, 비율/퍼센트)와 nullable 필드는 JSDoc 또는 기존 typedef로 명확히 한다.
- 파일 수정은 `apply_patch`를 사용한다. 파괴적 Git 명령과 광범위한 삭제는 명시적 요청 없이는 금지한다.

새 함수는 원칙적으로 60줄, 중첩 깊이 3, 순환 복잡도 10, 매개변수 5개를 넘지 않는다. 새 소스 파일은
500줄 이하를 권장한다. 기존 거대 파일을 수정할 때는 작업과 무관한 대규모 분리를 섞지 않되, 수정한
함수의 길이·중첩·복잡도를 악화시키지 않는다. guard clause, 계산 추출, 중복 제거를 우선한다.

### 오류 처리

오류를 빈 `catch`, 로그 없는 기본값, 광범위한 fallback으로 삼키지 않는다. 오류를 처리할 수 있는
경계는 사용자 입력/DOM 이벤트, 저장 역직렬화·마이그레이션, 네트워크·클라우드 저장, 최상위
부트스트랩이다. 낮은 계층은 복구 불가능한 오류를 문맥과 함께 전달한다.

경계에서 복구할 때는 실패한 작업을 식별할 신호를 남기고, 부분 갱신 상태를 성공처럼 진행하지 않으며,
제품 요구인 fallback만 테스트로 고정한다. 내부 불변식 위반을 `|| defaultValue`로 숨기지 않는다.
외부·저장 입력은 진입 경계에서 한 번 정규화하고 내부 호출부마다 같은 방어 로직을 복제하지 않는다.

## 4. 검증 계약

테스트는 구현 문자열이나 private 호출 횟수가 아니라 사용자가 관찰할 행동을 검증한다. 버그 수정은
수정 전 실패하고 수정 후 통과하는 회귀 검사를 남긴다. 관련되는 정상·경계·실패 경로를 선택해 다음을
확인한다.

1. 반환값과 변경되어야 할 상태가 정확히 바뀐다.
2. 관련 없는 상태는 유지되고 실패 시 부분 변경이 남지 않는다.
3. 저장, 로그, DOM, 이벤트 등 외부 효과가 필요한 횟수만 발생한다.
4. 같은 이벤트나 마이그레이션이 다시 실행돼도 중복 보상이 생기지 않는다.
5. 빈 입력, 누락 필드, 0과 한계 전후, 이전·손상 저장 중 계약상 가능한 사례가 안전하다.

mock은 시간·난수, 네트워크, 브라우저 스토리지, DOM/canvas 같은 외부 경계에서만 허용한다. 같은
도메인의 내부 helper를 mock하거나 실제 상태 전이를 성공값으로 대체하지 않는다. 내부 구현은 실제로
함께 실행하는 작은 통합 스모크를 우선한다.

관련 검사를 먼저 실행한 뒤 전체 검사를 실행한다.

```powershell
npm test
```

`npm test` 진입점이 사용할 수 없는 특별한 상황에서는 모든 `scripts/smoke-*.js`를 중단 없이 실행하고
그 이유를 변경 설명에 남긴다. UI, 이벤트, 스크롤, 드래그, 캔버스, 스크립트 순서가 달라졌다면 Node
스모크만으로 완료하지 않고 실제 페이지에서 해당 흐름과 콘솔 오류를 확인한다.

### PR 생성 전 상태 가드

PR을 생성하거나 사용자에게 PR 링크를 안내하기 직전에 반드시 아래 명령을 실행한다.

```powershell
npm run pr:check
```

검사가 기존의 열린·닫힌·병합된 PR을 보고하면 해당 브랜치로 새 PR을 만들거나 과거 PR 링크를 다시
안내하지 않는다. 특히 병합된 브랜치는 재사용하지 않고 최신 `origin/main`에서 새 브랜치를 만든 뒤
미반영 커밋만 옮긴다. PR 생성 후에는 GitHub에서 번호, `open` 상태, head와 base를 다시 확인한 뒤
그 결과의 링크만 안내한다.

## 5. 리뷰와 완료 정의

코드 리뷰는 Ponytail로 전체 흐름과 호출부를 추적한 뒤 다음을 우선 확인한다: 요구 행동의 정확성,
데이터 손실·보안·저장 호환성, 계층 역전과 숨은 전역, 경쟁·중복 적용, 실패 경로, 행동 기반 회귀 검사.
단순히 코드가 짧다는 이유로 정확성 문제를 낮게 평가하지 않는다. 발견 사항은 파일과 좁은 줄 범위,
재현 가능한 영향, 우선순위를 제시한다.

작업은 다음을 모두 만족할 때만 완료다.

- 요구 행동은 구현됐고 범위 밖 행동과 사용자 변경은 보존됐다.
- 중복 helper·shape·전역·의존이나 제거되지 않은 대체 구현이 없다.
- 상태 변경, 오류 처리, 저장 마이그레이션의 책임과 실패 방식이 명확하다.
- 관련 행동 회귀 검사와 `npm test`가 통과했다.
- UI 변경은 실제 브라우저에서 확인했다.
- 변경 설명에 위험, 초기화·저장 영향, 검증 명령과 결과를 짧게 남겼다.

세션 기억에만 존재해 다음 작업자가 추론해야 하는 계약은 계약이 아니다. 필요한 계약은 코드 구조,
JSDoc, 행동 테스트, 이 헌법 중 가장 알맞은 한 곳에 남긴다.
